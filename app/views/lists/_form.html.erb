<%= form_for(@list) do |f| %>
  <% if @list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% @list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <h2>people</h2>
 <%= f.fields_for :people do |person| %>
  <div class="people_fields">
  <div class="field">
    <%= person.label :name %><br />
    <%= person.text_field :name %><br />
    <%= person.label :budget %><br />
    <%= person.text_field :budget %>

    <%= person.check_box :_destroy %>
   <%= person.label :_destroy, 'remove' %>
  </div>
  </div>
 <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
